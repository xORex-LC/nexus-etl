# connector/infra/target/providers/ankey_rest/payloads

## Назначение

Зарезервировано для Ankey-специфичных payload-трансформеров (предобработка тела запроса перед отправкой). В текущей версии не используется — payload строится через `OperationApplyAdapter` в `datasets/apply_adapter.py`.
